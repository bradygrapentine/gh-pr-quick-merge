# v1.1 candidates wave

**Status:** ready
**Base SHA:** `207513e`
**Scope:** 5 stories from BACKLOG §1 v1.1-candidates table.
**Out of scope:** QM-177 PostHog telemetry (Epic 5 slim), QM-178 Safari port assessment (research only, no code).

## Stories

| ID | Title | Surface | Est |
|----|-------|---------|-----|
| QM-171 | Custom typed-confirmation modal for bulk close + label | content.js | S |
| QM-172 | Real label-picker dropdown (fetches repo labels) | new lib/label-picker.js + content.js | M |
| QM-173 | Sentry crash-reporting consent toggle | options pane + sentry-init wiring | S |
| QM-175 | Per-repo merge-template assignment UI | options templates pane | M |
| QM-176 | Update-branch poll instead of fixed 3-s wait | lib/auto-rebase.js or content.js | S |

## Mode: §3a Direct, single PR

File overlap (content.js touches 3 stories; options.html/js touches 2). Single track.

## Land order

1. **QM-176** (smallest, isolated) — replace `setTimeout(refresh, 3000)` with poll loop on `/pulls/:n` `behind_by`.
2. **QM-171** — extend `confirmBulkMergeTyped` (already exists) into a generic `confirmBulkActionTyped({title, items, action})`; reuse for close + label.
3. **QM-172** — new `lib/label-picker.js` exposing `pickLabels(repo, token, currentLabels)` → modal w/ checklist sourced from `GET /repos/:o/:r/labels`.
4. **QM-175** — options templates pane: per-repo binding picker (input + select-from-saved + save). Persist as `qm_templates: {[repo]: name}` (extend existing `qm_templates` shape).
5. **QM-173** — options Appearance pane (or new "Privacy" sub-section): Sentry on/off toggle. `chrome.storage.sync.qm_sentry_consent`. `sentry-init.js` reads it before booting.

## Files

```
NEW:
  lib/label-picker.js
  test/label-picker.test.js

MODIFIED:
  content.js                (QM-171, QM-172, QM-176)
  options.html              (QM-173, QM-175)
  options.js / options-shell.js (QM-175, QM-173)
  lib/sentry-init.js        (QM-173 — read consent gate)
  lib/auto-rebase.js or content.js   (QM-176 polling)
  styles.css / theme.css    (label-picker modal, consent toggle)
```

## Acceptance

- 5 stories ship per BACKLOG note column.
- `npm test` green; coverage ≥ 95%.
- All e2e + visual still green (no UI regressions).
- Manual smoke: bulk close 5 PRs → typed confirmation; label picker fetches real labels; toggle off Sentry → boot returns `{booted:false, reason:"no-consent"}`; per-repo template binding saves; Update button polls until `behind_by=0`.
